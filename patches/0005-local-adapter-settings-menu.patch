diff --git a/app/src/app_menus.rs b/app/src/app_menus.rs
index 03c14be..295d36d 100644
--- a/app/src/app_menus.rs
+++ b/app/src/app_menus.rs
@@ -11,6 +11,7 @@ use crate::server::telemetry::LaunchConfigUiLocation;
 use crate::settings::{
     AISettings, BlockVisibilitySettings, DebugSettings, DefaultSessionMode, SelectionSettings,
 };
+use crate::settings_view::SettingsSection;
 use crate::terminal::alt_screen_reporting::AltScreenReporting;
 use crate::terminal::session_settings::SessionSettings;
 use crate::terminal::settings::{SpacingMode, TerminalSettings};
@@ -548,10 +549,11 @@ fn make_new_ai_menu(ctx: &AppContext) -> Menu {
         items.push(MenuItem::Separator);
         items.push(MenuItem::Custom(CustomMenuItem::new(
             "Local Adapter Settings...",
-            move |_ctx| {
-                let _ = std::process::Command::new("open")
-                    .arg("http://127.0.0.1:18888/settings")
-                    .spawn();
+            |ctx| {
+                ctx.dispatch_global_action(
+                    "root_view:open_settings_page_in_new_window",
+                    &SettingsSection::LocalAdapter,
+                );
             },
             no_updates,
             None,
